# Default example

Minimal end-to-end example: creates an RG, VNet, subnet, Premium ACR with public access disabled, and the agent pool.

```bash
terraform init
terraform apply
```

Deletes cleanly with `terraform destroy`.
